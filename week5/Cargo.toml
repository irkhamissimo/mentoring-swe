[package]
name = "todolist"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono= {version = "0.4.42", features = ["serde"]}
serde = {version = "1", features = ["derive"]}
serde_json = "1.0"
clap = { version = "4", features = ["derive"] }
